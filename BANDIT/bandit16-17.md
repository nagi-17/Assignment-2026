Level 16-17

the command nmap seemed useful because it says map and i need to find specific servers

nmap hostname   scapns host and gives tere detail
    nmap -p from_no.-to_no.(or specific if needed) hostname scan from to

then manually used ncat --ssl localhost port.no

got private key

pasted it in the previously made folder

logged in using 
ssh -i privatekey username@hostname -p port


Pass
